import { test, expect} from '@playwright/test';
function cargarPagina(url: string): Promise<string>{
    return new Promise(
        async (res,req)=>{
            //const page = await browser.newPage();
            //await page.
        }
    );
}